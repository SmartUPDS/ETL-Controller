All resources (Input, Output, Mappings, intermediate resources) are added here. 

There is a predefined fodler structure which can be created from ETL_Controller  ( com.smartupds.etlcontroller.etl.controller.Controller.createFoldersStructure() )

